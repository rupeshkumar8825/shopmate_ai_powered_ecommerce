import { useAuth } from "../hooks/use.auth.hook"

// this is the login page for the admin it seems 
export const LoginPage = () => {
    // all the recoil states and selectors come here 
    


    // all the states related to this component comes here

    // all the pre defined or custom made  hooks like the useeffect states comes here
    // const { isAuthenticated, loading, user } = useAuth();

    return (
        <div>
            Hi welcome to the login page 
        </div>
    )
}