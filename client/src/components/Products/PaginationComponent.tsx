// pagination component to show the pages buttons depending upon user is on which page and so on

export const PaginationComponent = () => {
    return (
        <div>
            Here comes the pagination related buttons showing the different 
            page numbers where we can navigate directly to for this purpose
        </div>
    )
}