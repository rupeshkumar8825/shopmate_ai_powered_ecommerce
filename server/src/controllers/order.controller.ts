import { Request, Response } from "express";

// controller layer code to handle everything related to the orders 
export class OrderController {
    
    static async placeMeOrder (request : Request, response : Response) {
        
    }
}